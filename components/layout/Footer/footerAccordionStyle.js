const footerAccordionStyle = (theme) => ({
  accordionHeader: {
    backgroundColor: theme.palette.black,
    color: theme.palette.white,
    fontWeight: '800',
    fontSize: '18px',
  },
  accordionList: {
    backgroundColor: theme.palette.black,
    display: 'flex',
    flexDirection: 'column',
  },
});

export default footerAccordionStyle;
